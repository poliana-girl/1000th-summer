#include <iostream>
#include "../../libs/AudioFile/AudioFile.h"

AudioFile<double> phaser(AudioFile<double> wav) {
    //TODO: write function that takes 2 channel input and produces a phase effect by delaying either the left or right channel by a bit
    return wav;
}